{
    "private": true,
    "scripts": {
        "u": "git pull && cargo upgrade --allow-prerelease --workspace",
        "notedown": "cargo upgrade --workspace notedown_ast",
        "p": "cargo release --execute",
        "test": "cargo test --release",
        "fmt": "cargo fmt --all",
        "push": "wee test && git push",
        "reset": "git reset Head~ --soft"
    }
}
